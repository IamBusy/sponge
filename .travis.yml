language: python
python:
  - "3.3"
  - "3.4"
  - "3.5"
  - "3.6"
# command to install dependencies
install:
  - pip install -r requirements.txt
  - pip install codecov
# command to run tests
script:
  - python -m unittest discover

after_success:
  - codecov --token=274c5a8c-66de-4909-8750-a98a8f462610

services:
  - redis-server
